const BASE_PATH = "/";

/**
 * Used to locate the "About" page
 * @type {string}
 */
export const PATH_ABOUT = BASE_PATH + 'about';

/**
 * Used to locate the "Clash of the Titans" page
 * @type {string}
 */
export const PATH_CLASH = BASE_PATH + 'clash-of-the-titans';

/**
 * Used to locate the "experience" page
 * @type {string}
 */
export const PATH_EXPERIENCE = BASE_PATH + 'experience';

/**
 * Used to locate the "Home" page
 * @type {string}
 */
export const PATH_HOME = BASE_PATH;

/**
 * Used to locate the "portfolio" page
 * @type {string}
 */
export const PATH_PORTFOLIO = BASE_PATH + 'portfolio';